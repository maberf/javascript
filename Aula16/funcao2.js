function soma (n1=0,n2=0) { // Os zeros são parâmetros opcionais, usados caso indefinição.
    return n1 + n2          // Sem parâmetros opcionais, a indefinição gra NaN.
}
console.log(soma(2,3)) // Dois parâmetros, 2 e 3.