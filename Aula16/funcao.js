function parimp (n) { // n é o parâmetro formal
    if (n % 2 == 0) {
        return 'par' // ação 1
    } else {
        return 'ímpar' // ação 2
    } // o retorno será par ou ímpar. Só pode ter 1 retorno.
}
console.log(parimp(236)) // Chamada. 11 é o parâmetro.